"""
列表解析虽然代码简短，但是本质上还是和for命令，而Python的for命令效率并不高，而map()函数实现了相同功能，并且效率更高，原则上来说，它的循环命令速度相当于C语言。
"""

>>> a = [1, 2, 3, 4, 5]
>>> b = map(lambda x: x+2, a)
>>> b
<map object at 0x000001E21018E4E0>
>>> list(b)
[3, 4, 5, 6, 7]
>>> c = map(lambda x, y: x*y, a, b)
>>> list(c)
[3, 8, 15, 24, 35]


